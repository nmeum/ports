#!/bin/ksh

daemon="${TRUEPREFIX}/bin/xandikos --autocreate -d ${LOCALSTATEDIR}/xandikos"
daemon_user="_xandikos"

. /etc/rc.d/rc.subr

pexp="${MODPY_BIN} ${daemon}${daemon_flags:+ ${daemon_flags}}"
rc_reload=NO
rc_bg=YES

rc_cmd $1
